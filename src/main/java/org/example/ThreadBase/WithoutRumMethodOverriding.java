package org.example.ThreadBase;

/**
 *  if we are not overriding run() then thread class run() excuted which have empty implementation
 *   hence we won't get any output
 */
public class WithoutRumMethodOverriding extends Thread{
}
